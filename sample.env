# Database configuration
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=your_db_name

# Redis configuration
REDIS_HOST=your_redis_host
REDIS_PORT=6379

# Go backend
PORT=8080

# Python backend
# (uvicorn default port is 8000, so no need to set unless you want to override) 
